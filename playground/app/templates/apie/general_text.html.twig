<p>
You are now viewing the CMS dashboard in the bounded context {{ boundedContext.id.toNative() }}, but you can also see the <a href="{{ url('apie.' ~ boundedContext.id.toNative() ~ '.swagger_ui') }}">Rest API generated Swagger UI page.</a>
</p>
<h2>Current settings:</h2>
<div>
  <gr-button variant="secondary" href="{{ url('apie.example.call-method-commit-PlaygroundConfiguration-resetConfiguration') }}">Reset config</gr-button>
  <gr-button variant="primary" href="{{ url('apie.example.call-method-commit-PlaygroundConfiguration-applyConfiguration') }}">Change config</gr-button>

</div>
<p>
  {{ renderApieCmsData(configuration) }}
</p>