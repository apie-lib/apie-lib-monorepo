This is a the default dashboard template.

You can override this in config/packages/apie.yaml.

<blockquote><pre>apie:
    cms:
        dashboard_template: 'apie/dashboard.html.twig'</pre></blockquote>

Now create a template in your project in templates/apie/dashboard.html.twig without the HTML layout and you have
your own custom dashboard!