<apie-single-input
    {% if (property('label')) %}
    label="{{ translate(property('label')) }}"
    {% endif %}
    types="{{ property('type') }}"
    name="{{ property('name') }}"
    {% for key, value in property('additionalAttributes') %}
    {{ key }}="{{ value }}"
    {% endfor %}
    ></apie-single-input>
