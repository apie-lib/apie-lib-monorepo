{% if property('groupName').hasChildFormFieldName() %}
<apie-form-group-definition name="{{ property('groupName').getChildFormFieldName() }}" label="{{ property('groupName') }}">
{% endif %}
  {% for key in property('keys') %}
       {{ component(key) }}
  {% endfor %}
  {%- if property('validationError') -%}
  {#TODO#}
  {%- endif -%}
{% if property('groupName').hasChildFormFieldName() %}
</apie-form-group-definition>
{% endif %}