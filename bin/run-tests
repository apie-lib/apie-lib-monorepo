#!/usr/bin/env bash
rm -rf /tmp/cache
composer install && php -m && php -d memory_limit=-1 -d pcov.enabled=1 -d pcov.directory=. vendor/bin/phpunit -vvv --coverage-clover=$1.xml --coverage-text --coverage-html=$1 --testdox